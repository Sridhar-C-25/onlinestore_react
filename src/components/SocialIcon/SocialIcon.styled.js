import styled from "styled-components";

export const IconContainer = styled.div`
  display: flex;
  gap: 1rem;

  div {
    background: var(--pink-col);
    display: flex;
    padding: 0.5rem;
    cursor: pointer;
    border-radius: 50%;
  }
`;
